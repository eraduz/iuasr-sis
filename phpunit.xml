<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- De volledige suite piekt boven de standaard 128M (o.a. door de
             seeders per test); ruim genoeg zetten zodat 'php artisan test' werkt. -->
        <ini name="memory_limit" value="1024M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- Tests draaien op een aparte MariaDB-testdatabase (zelfde InnoDB/FK-gedrag
             als productie); nooit de ontwikkel- of productiedatabase, nooit echte data.

             DB_DATABASE staat hier BEWUST hard: RefreshDatabase draait migrate:fresh
             en zou de ontwikkeldatabase wissen als deze regel ontbreekt of misgaat.
             Deze waarde mag dus nooit van een gegenereerd bestand afhangen.

             HOST/POORT/GEBRUIKER/WACHTWOORD staan hier BEWUST NIET: die verschillen
             per machine (de RDP heeft bijvoorbeeld geen databasewachtwoord). Ze komen
             uit de .env / .env.testing van de machine zelf. Stonden ze hier vast, dan
             falen de tests overal waar de inloggegevens anders zijn - met een
             misleidende 'Access denied' die niets met de tests te maken heeft. -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="iuasr_sis_test"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
    </php>
</phpunit>
